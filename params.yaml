experiment:
  grid_size: 3
  episodes: 100
  max_steps: 20
  planning_horizon: 15
  inference_iterations: 10
  seed: 0
  fov_size: 3
  damping: 0.75

methods:
  bp:
    planning_iterations: 1
  loopy:
    planning_iterations: 10
  region-extended:
    planning_iterations: 10
  reduced-aif:
    planning_iterations: 10
  nuijten:
    planning_iterations: 10
  reduced-nuijten:
    planning_iterations: 10
